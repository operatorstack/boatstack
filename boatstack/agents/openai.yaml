interface:
  display_name: "Boatstack"
  short_description: "Plan, build, verify, review, and ship with evidence."
  default_prompt: "Use $boatstack to turn this product request into a question-led, evidence-gated implementation loop."
