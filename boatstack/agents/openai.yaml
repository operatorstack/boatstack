interface:
  display_name: "Boatstack"
  short_description: "Use one V2 kernel for delivery state, authority, effects, and proof."
  default_prompt: "Use $boatstack V2. Read status once, follow the returned semantic transition, preserve exact goal and invocation identity, and never infer authority or completion from files, branches, authentication, or prior conversation."
