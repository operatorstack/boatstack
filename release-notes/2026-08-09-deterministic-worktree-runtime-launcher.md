### Make Boatstack commands reliable in every worktree

Every generated repository now tracks pinned POSIX and PowerShell launchers. A fresh linked worktree can activate its exact Boatstack runtime before hook trust, without selecting a helper from another worktree or treating missing local bytes as an available update.
