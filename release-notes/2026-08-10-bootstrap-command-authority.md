### Bind planning bootstrap to the selected workspace

New feature planning now resolves its literal write command from the current embedded or detached workspace, verifies the selected in-repository source plan, and returns one shell-specific envelope that works consistently across supported coding hosts.
