### Keep configuration mutations verified
Commands that update Boatstack configuration now preserve the declared source and every generated projection, so repeated delivery exclusions and capability registrations no longer invalidate detached supervision.
