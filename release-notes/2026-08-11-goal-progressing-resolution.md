### Keep goal resolution and operation discovery aligned

Untargeted coding-agent delivery runs now avoid already-satisfied and explicitly
requested maintenance transitions, preserve authority frontiers, and keep
plan-slice markers from implying publication order. Every enabled interactive
host now discovers exactly three Boatstack operations: Autoplan, Run, and
Update. Installation and updates preserve user-owned collisions instead of
overwriting them.
