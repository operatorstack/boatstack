### Keep maintenance independent from product intent

Boatstack maintenance now binds the verified durable product-goal state as preserved context instead of requiring or creating a delivery goal. Updates and their recovery paths preserve a known goal, preserve verified absence, reject unknown goal evidence, and keep command-scoped product intent outside maintenance admission.
