### Guard the kernel architecture boundary

Repository checks now prevent software-delivery dependencies and fixtures from entering the general kernel, exercise race detection on Linux, and keep the pending production-runtime migration visible.
