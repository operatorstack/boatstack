### Resolve foreground-work inputs from committed results

Flows can bind a foreground-work input to a required output from an earlier Work transition. Boatstack resolves that input from the applicable committed journal record. The downstream result keeps the exact producer and byte provenance.
